from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description=' The goal of the project is to create a generative chatbot that is based on GPT2 and fined tuned on therapy t transcripts and other text data.',
    author='Jason Kim',
    license='MIT',
)
